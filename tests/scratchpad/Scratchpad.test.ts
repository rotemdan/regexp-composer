import { describe, test, expect } from 'vitest'
import fs from 'fs'

import * as R from '../../src/exports/Exports.ts'

// Agents: use this to run temporary tests, and any file output to `tests/out/`

const baseOutPath = 'tests/out/'

describe('Scratchpad', () => {
	test('empty', () => {
	})
})
